'use strict';

describe('myApp.userlist module', function() {

    beforeEach(module('myApp.userlist'));

    describe('userlist controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var userlistCtrl = $controller('UserlistCtrl');
            expect(userlistCtrl).toBeDefined();
        }));

    });
});
