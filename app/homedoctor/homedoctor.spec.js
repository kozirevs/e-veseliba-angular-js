'use strict';

describe('myApp.homedoctor module', function() {

    beforeEach(module('myApp.homedoctor'));

    describe('homedoctor controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var homedoctorCtrl = $controller('HomedoctorCtrl');
            expect(homedoctorCtrl).toBeDefined();
        }));

    });
});
